#!/usr/bin/env groovy
node {

    stage('Checkout SCM') {
        checkout scm
    }
    
    stage('Build') {
        sh 'mvn clean install -Phomolog'
    }
    
    stage('SonarQube') {
        sh 'mvn sonar:sonar'
    }
   
}
    
